setup:
  addons:
    - plan: heroky-postgresql

build:
  docker:
    web: Dockerfile

release:
  image: web
  command:
    - python manage.py collectstatic --noinput

run:
  web: gunicorn --chdir bookstore_project --log-file - bookstore_project.wsgi
